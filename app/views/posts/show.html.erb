<div class="container">
  <div class="row">
    
    <div class="col-sm-6">
      <!-- current_userは名前がどのユーザもloginUserになってしまうので使用不可-->
      < %= render "users/users", user: @user %>
    </div>
    
    <div class="col-sm-6">
      <div><%= @post.title %></div>
      <% if @post.image? %>
        <%= image_tag @post.image.url, :size =>'500x500' %>
      <% end %>
      <div><%= @post.created_at %></div>
      <%= render "favorites/likes_button", post: @post %>
      <%= @post.favorites.count %>
    </div>
    
  </div>
</div>
